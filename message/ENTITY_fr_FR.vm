${SHARP}----------------------------------------------------------------------------------------------------------
${SHARP}--- Messages in FRENCH
${SHARP}--- File generated by $generator.name ( version $generator.version ) : $today.date ( Time $today.time )
${SHARP}----------------------------------------------------------------------------------------------------------


${SHARP}----- $entity.name messages
subscribe_title=information $entity.name

${SHARP}----- $entity.name attributs
#foreach( $field in ${entity.attributes} )
${field.formattedName(0)}=${field.label}
#end

${SHARP}----- $entity.name actions
add=Ajouter $entity.name
detail=Afficher les details
delete=Supprimer $entity.name
delete_confirm=Etes vous certain de vouloir supprimer Ce/Cette $entity.name ?

${SHARP}----- $entity.name erreur
erreur_duplicate=Erreur : $fn.argumentsList($entity.keyAttributes) existe d\u00e9j\u00e0