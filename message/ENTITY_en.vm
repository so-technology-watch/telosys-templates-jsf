${SHARP}----------------------------------------------------------------------------------------------------------
${SHARP}--- Messages in ENGLISH ( the default language ) 
${SHARP}--- File generated by $generator.name ( version $generator.version ) : $today.date ( Time $today.time )
${SHARP}----------------------------------------------------------------------------------------------------------


${SHARP}----- User messages
subscribe_title=Inscription

${SHARP}----- User attributs
#foreach( $field in ${entity.attributes} )
${field.formattedName(0)}=${field.label}
#end

${SHARP}----- User actions
add=Add $entity.name
detail=Show the details
delete=Remove $entity.name
delete_confirm=Are you sure you want to delete This $entity.name ?

${SHARP}----- User erreur
erreur_duplicate=Erreur : $entity.name already exists